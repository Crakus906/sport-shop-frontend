import React from "react";
import { useDispatch, useSelector } from "react-redux";

const Home = () => {
  const dispatch = useDispatch();

  return (
    <div>
      <div>homeasdcas</div>
    </div>
  );
};

export default Home;
